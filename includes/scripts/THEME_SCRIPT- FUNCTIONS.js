jQuery(document).ready(function($){

	/*
	//Use fancybox if plugin is included
	if($('.fancybox').length > 0 && jQuery.fancybox){
		$(".fancybox").fancybox({
			maxWidth: 1000,
			padding: 0,
			helpers: {
				overlay: {
					locked: false
				}
			}
		});
	}

	// Use Placeholders if plugin is included
	if($('input[placeholder], textarea[placeholder]').length > 0 && jQuery.setPlaceholders)
		$('input[placeholder], textarea[placeholder]').setPlaceholders();


	//Use Slicknav if plugin is inlcuded
	if($('#main-menu').length > 0 && ('#slickmenu').length > 0 && jQuery().slicknav){
		$('#main-menu').slicknav({
			prependTo: '#slickmenu',
			label: '',
			closedSymbol: '<i class="fa fa-chevron-down">',
			openedSymbol: '<i class="fa fa-chevron-up">'
		});
	}


	function scrollPagelink($theDiv){
		if($theDiv.length > 0){
			$('html,body').stop(true,true).animate({
					scrollTop: $theDiv.offset().top},
				700);
		}
	}

	var $hash = window.location.hash;
	if($hash)
		scrollPagelink($('#'+ $hash.substring(1, $hash.length)));

	$('a.page-link').click(function(){
		scrollPagelink($( $(this).attr('href') ));
	})
	*/

});